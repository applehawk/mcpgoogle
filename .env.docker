# Environment variables for Docker Compose Multi-User setup
# Copy this file to .env in the same directory as docker-compose.multi-user.yml

# ============================================================================
# Google OAuth Credentials (shared across all users)
# ============================================================================
# Get these from Google Cloud Console: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ============================================================================
# User Credentials (username/password pairs)
# JWT tokens are obtained AUTOMATICALLY at container startup
# ============================================================================

# Alice's credentials
ALICE_USERNAME=alice
ALICE_PASSWORD=alice_secure_password_123

# Bob's credentials
BOB_USERNAME=bob
BOB_PASSWORD=bob_secure_password_456

# Carol's credentials
CAROL_USERNAME=carol
CAROL_PASSWORD=carol_secure_password_789

# Dave's credentials
DAVE_USERNAME=dave
DAVE_PASSWORD=dave_secure_password_abc

# Eve's credentials
EVE_USERNAME=eve
EVE_PASSWORD=eve_secure_password_def

# ============================================================================
# How it works:
# ============================================================================
# 1. Each container starts and runs docker-entrypoint.sh
# 2. Entrypoint script calls: POST /auth/login with username/password
# 3. OMA Backend returns JWT access_token
# 4. Token is automatically exported as OMA_ACCESS_TOKEN environment variable
# 5. MCP Hub uses this token to fetch Google credentials
#
# Benefits:
# - No manual JWT token management
# - Tokens refresh automatically on container restart
# - Passwords stored securely (not JWT tokens)
# - Standard username/password authentication

# ============================================================================
# To add more users:
# ============================================================================
# 1. Register user in OMA Backend:
#    curl -X POST https://rndaibot.ru/api/v1/auth/register \
#      -d '{"username": "frank", "email": "frank@example.com", "password": "pass"}'
#
# 2. User connects Google account via web interface
#
# 3. Add to this .env file:
#    FRANK_USERNAME=frank
#    FRANK_PASSWORD=frank_password
#
# 4. Add service to docker-compose.multi-user.yml
#
# 5. Run: docker-compose -f docker-compose.multi-user.yml up -d

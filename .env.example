# ============================================================================
# MCP Google Hub - Environment Configuration
# ============================================================================

# ----------------------------------------------------------------------------
# Authentication Mode
# ----------------------------------------------------------------------------
# Choose authentication mode:
# - "oma_backend" (recommended): Use OMA Backend for centralized OAuth management
# - "local_file" (legacy): Use local credentials.json and token.json files
AUTH_MODE=oma_backend

# ----------------------------------------------------------------------------
# OMA Backend Configuration (for AUTH_MODE=oma_backend)
# ----------------------------------------------------------------------------

# OMA Backend base URL
OMA_BACKEND_URL=https://rndaibot.ru/api/v1

# User access token for OMA backend authentication
# Get this token by logging in to the OMA backend web interface
# Example: Login at https://rndaibot.ru -> Get token from /api/auth/login
OMA_ACCESS_TOKEN=your_oma_access_token_here

# SSL verification (set to false for development with self-signed certificates)
OMA_VERIFY_SSL=true

# Google OAuth Client ID and Secret
# These must match the credentials configured in OMA backend
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ----------------------------------------------------------------------------
# Legacy Local File Authentication (for AUTH_MODE=local_file)
# ----------------------------------------------------------------------------

# Path to Google OAuth credentials file (from Google Cloud Console)
GOOGLE_CREDENTIALS_PATH=secrets/credentials.google.json

# Path to store OAuth token after authentication
GOOGLE_TOKEN_PATH=data/token.google.json

# ----------------------------------------------------------------------------
# Google API Scopes (Optional - Override defaults)
# ----------------------------------------------------------------------------

# Space-separated list of Google API scopes
# Defaults to Gmail, Calendar, and Drive access
# Uncomment to override:
# GOOGLE_SCOPES=https://www.googleapis.com/auth/gmail.send https://www.googleapis.com/auth/calendar.events

# ============================================================================
# MCP Server Configuration (for OpenAI Agents Integration)
# ============================================================================

# ----------------------------------------------------------------------------
# MCP Authentication
# ----------------------------------------------------------------------------
# Bearer token for MCP endpoint authentication (optional)
# Leave empty to disable authentication (development only)
# Generate a secure random token: openssl rand -hex 32
MCP_AUTH_TOKEN=

# ----------------------------------------------------------------------------
# MCP CORS Configuration
# ----------------------------------------------------------------------------
# Comma-separated list of allowed origins for CORS
# Use "*" to allow all origins (dev only), or specify domains in production
# Examples:
#   - Development: MCP_CORS_ORIGINS=*
#   - Production: MCP_CORS_ORIGINS=https://api.openai.com,https://yourapp.com
MCP_CORS_ORIGINS=*

# ----------------------------------------------------------------------------
# MCP Server Network Configuration
# ----------------------------------------------------------------------------
# Host to bind the MCP server (0.0.0.0 for all interfaces)
MCP_HOST=0.0.0.0

# Port for MCP server
MCP_PORT=8000

# Transport type: "streamable-http" (recommended for OpenAI) or "http"
MCP_TRANSPORT=streamable-http
